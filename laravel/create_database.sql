-- Create database for MDHT Booking System
CREATE DATABASE IF NOT EXISTS mdht_booking CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Select the database
USE mdht_booking;

-- Show success message
SELECT 'Database mdht_booking created successfully!' AS message;
